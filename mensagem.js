const arrMensagem = ['Pedido confirmado!', 'Pedido aprovado!', 'Pedido enviado!']; // array de mensagens

// apareceNaTela.forEach((arrMensagem) => { //
//     console.log(arrMensagem)
// })
// apareceNaTela(arrMensagem)

arrMensagem.forEach((msg) => {
    console.log(msg)

})
